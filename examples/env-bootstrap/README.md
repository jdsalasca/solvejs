# Env Bootstrap Example

Executable startup config example using `@jdsalasc/solvejs-env`.

Run from repo root:

```bash
npm run build
npm run example:env
npm run example:env:test
```

Docs: `docs/problems/env-config.html`
