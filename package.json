{
  "name": "solvejs-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Solve common JavaScript and TypeScript problems with lightweight utilities.",
  "license": "MIT",
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:core && npm run --workspace packages/solvejs build",
    "build:core": "npm run --workspace packages/solvejs-date build && npm run --workspace packages/solvejs-string build && npm run --workspace packages/solvejs-list build && npm run --workspace packages/solvejs-regex build && npm run --workspace packages/solvejs-constants build && npm run --workspace packages/solvejs-numbers build && npm run --workspace packages/solvejs-validators build && npm run --workspace packages/solvejs-objects build && npm run --workspace packages/solvejs-async build && npm run --workspace packages/solvejs-env build",
    "test": "npm run test:core && npm run --workspace packages/solvejs test",
    "test:core": "npm run --workspace packages/solvejs-date test && npm run --workspace packages/solvejs-string test && npm run --workspace packages/solvejs-list test && npm run --workspace packages/solvejs-regex test && npm run --workspace packages/solvejs-constants test && npm run --workspace packages/solvejs-numbers test && npm run --workspace packages/solvejs-validators test && npm run --workspace packages/solvejs-objects test && npm run --workspace packages/solvejs-async test && npm run --workspace packages/solvejs-env test",
    "lint": "npm run --workspaces lint",
    "benchmark": "npm run build && node benchmarks/index.mjs",
    "clean": "npm run --workspaces clean",
    "inventory": "node scripts/generate-package-inventory.mjs",
    "report:perf": "node scripts/generate-performance-report.mjs",
    "metrics:community": "node scripts/generate-community-metrics.mjs",
    "growth:posts": "node scripts/generate-growth-posts.mjs",
    "test:baseline": "node scripts/check-test-baseline.mjs",
    "docs:check-links": "node scripts/check-doc-links.mjs",
    "example:next": "node examples/nextjs-route-handler/index.mjs",
    "example:next:test": "node --test examples/nextjs-route-handler/test.mjs",
    "example:express": "node examples/express-middleware/index.mjs",
    "example:express:test": "node --test examples/express-middleware/test.mjs",
    "example:env": "node examples/env-bootstrap/index.mjs",
    "example:env:test": "node --test examples/env-bootstrap/test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
