name: Publish Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        workspace:
          - packages/solvejs-date
          - packages/solvejs-string
          - packages/solvejs-list
          - packages/solvejs-regex
          - packages/solvejs-constants
          - packages/solvejs-numbers
          - packages/solvejs-validators
          - packages/solvejs-objects
          - packages/solvejs-async
          - packages/solvejs
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Check npm token
        id: token
        run: |
          if [ -z "${NODE_AUTH_TOKEN}" ]; then
            echo "NPM_TOKEN is missing. Skipping publish for this run."
            echo "has_token=false" >> $GITHUB_OUTPUT
          else
            echo "has_token=true" >> $GITHUB_OUTPUT
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Install
        if: steps.token.outputs.has_token == 'true'
        run: npm install

      - name: Build
        if: steps.token.outputs.has_token == 'true'
        run: npm run build

      - name: Resolve package metadata
        if: steps.token.outputs.has_token == 'true'
        id: package
        run: |
          NAME=$(node -p "require('./${{ matrix.workspace }}/package.json').name")
          VERSION=$(node -p "require('./${{ matrix.workspace }}/package.json').version")
          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if version exists
        if: steps.token.outputs.has_token == 'true'
        id: registry
        run: |
          set +e
          npm view "${{ steps.package.outputs.name }}@${{ steps.package.outputs.version }}" version --json > /tmp/version.json 2>/tmp/npm-view.err
          RESULT=$?
          set -e
          if [ $RESULT -eq 0 ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Version already exists on npm, skipping publish."
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Version not found on npm, publishing."
          fi

      - name: Publish workspace
        if: steps.token.outputs.has_token == 'true' && steps.registry.outputs.exists != 'true'
        run: npm publish --workspace "${{ matrix.workspace }}" --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        if: steps.token.outputs.has_token == 'true' && matrix.workspace == 'packages/solvejs'
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
